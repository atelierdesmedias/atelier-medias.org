{#
 * @title: "AdmPresentation" TWIG view component
 * @description:
 #}

<div class="AdmPresentation {{ classElement }}">
    {# TODO à placer en back #}

    <section class="AdmPresentation_wrapper">
        <div class="AdmPresentation_content">
            <h1 class="AdmPresentation_title">{{ dico.components.AdmPresentation.title }}</h1>

            <div class="AdmPresentation_text">
                {# TODO text en ADMIN depuis la page "home" #}
                {% for paragraph in dico.components.AdmPresentation.paragraphs %}
                    <p>{{ paragraph }}</p>
                {% endfor %}
            </div>
        </div>
    </section>
</div>

